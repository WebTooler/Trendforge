import fs from 'node:fs';
import { available, mark, markSuccess } from './ai-provider-router.mjs';
import { buildWriterContract, validateDraft } from './trendforge-editorial-policy.mjs';
import { validatePoliticalNeutrality } from './political-neutrality-guard.mjs';

const MAX_TRANSIENT_RETRIES=1;
const MAX_PROVIDER_ATTEMPTS_PER_RUN=12;
const MAX_PROVIDER_ATTEMPTS_PER_CANDIDATE=2;
const candidateAttemptCap=()=>{const index=Number(process.env.TRENDFORGE_WRITER_CANDIDATE_INDEX||1);const queueSize=Number(process.env.TRENDFORGE_WRITER_QUEUE_SIZE||1);return index<=2||queueSize<=2?2:1;};
const MAX_REPAIR_PROVIDER_ATTEMPTS=4;
const MIN_WRITER_WORDS=180;
const WRITER_TARGET_MIN_WORDS=500;
const WRITER_TARGET_MAX_WORDS=700;
const writerBudgetPath='data/ai-run-budget.json';
const repairBudgetPath='data/ai-repair-run-budget.json';
const runKey=process.env.GITHUB_RUN_ID||`local-${new Date().toISOString().slice(0,10)}`;
const loadBudget=(kind='writer')=>{const budgetPath=kind==='repair'?repairBudgetPath:writerBudgetPath;try{const raw=JSON.parse(fs.readFileSync(budgetPath,'utf8'));return raw?.runKey===runKey&&Number.isFinite(raw?.attempts)?raw:{runKey,attempts:0,updatedAt:new Date().toISOString()};}catch{return{runKey,attempts:0,updatedAt:new Date().toISOString()};}};
const saveBudget=(budget,kind='writer')=>{const budgetPath=kind==='repair'?repairBudgetPath:writerBudgetPath;fs.mkdirSync('data',{recursive:true});budget.updatedAt=new Date().toISOString();fs.writeFileSync(budgetPath,JSON.stringify(budget,null,2)+'\n');};
const providerTimeout=provider=>({OpenRouter:40000,Cohere:35000,Groq:35000,Gemini:35000}[provider]||35000);
const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));
const parseRateResetMs=value=>{const raw=String(value||'').trim().toLowerCase();if(!raw)return 0;const minutes=raw.match(/(\d+(?:\.\d+)?)\s*m/),seconds=raw.match(/(\d+(?:\.\d+)?)\s*s/),ms=raw.match(/(\d+(?:\.\d+)?)\s*ms/);if(ms)return Number(ms[1]);if(minutes||seconds)return (Number(minutes?.[1]||0)*60+Number(seconds?.[1]||0))*1000;const numeric=Number(raw);return Number.isFinite(numeric)&&numeric>0?numeric*1000:0;};
const retryDelay=(response,attempt)=>{const retryAfter=parseRateResetMs(response.headers.get('retry-after'));const tokenReset=parseRateResetMs(response.headers.get('x-ratelimit-reset-tokens')||response.headers.get('ratelimit-reset-tokens'));const requestReset=parseRateResetMs(response.headers.get('x-ratelimit-reset-requests')||response.headers.get('ratelimit-reset-requests'));if(response.status===429){const reset=retryAfter||tokenReset||requestReset;if(reset)return Math.min(reset+500,65000);return Math.min(15000*(attempt+1),30000);}return Math.min(2500*(2**attempt)+Math.floor(Math.random()*500),12000);};
const keyFor=provider=>({Groq:'GROQ_API_KEY',Gemini:'GEMINI_API_KEY',OpenRouter:'OPENROUTER_API_KEY',Cohere:'COHERE_API_KEY'}[provider]);
const providerModel=provider=>({Groq:process.env.GROQ_MODEL||'openai/gpt-oss-20b',Gemini:process.env.GEMINI_MODEL||'gemini-3.6-flash',OpenRouter:process.env.OPENROUTER_MODEL||'google/gemma-4-31b-it:free',Cohere:process.env.COHERE_MODEL||'command-a-plus-05-2026'}[provider]);
const articleSchema={type:'object',properties:{title:{type:'string'},description:{type:'string'},content:{type:'string'}},required:['title','description','content'],additionalProperties:false};
const articleFormats={OpenRouter:{type:'json_object'},Groq:{type:'json_schema',json_schema:{name:'trendforge_article',strict:true,schema:articleSchema}},Cohere:{type:'json_object',schema:articleSchema}};
const repairSchema={type:'object',properties:{repairs:{type:'array',items:{type:'object',properties:{original:{type:'string'},replacement:{type:'string'}},required:['original','replacement'],additionalProperties:false}}},required:['repairs'],additionalProperties:false};
const repairFormats={OpenRouter:{type:'json_object'},Groq:{type:'json_schema',json_schema:{name:'trendforge_repairs',strict:true,schema:repairSchema}},Cohere:{type:'json_object',schema:repairSchema}};
const commonSystem='You are the TrendForge Writer Engine. Follow the editorial policy in the prompt. The retrieved evidence pack is the sole factual source. Every material factual sentence must be directly supported by one or more supplied evidence passages. Never fill gaps with model memory, common knowledge, inference, assumptions, plausible details, or unstated context. Preserve factual polarity exactly: never reverse, weaken, strengthen, or otherwise change the direction of an evidence-backed fact. If evidence says increased, do not write decreased; if reduced, do not write increased; likewise preserve rise/fall, gain/loss, approve/reject, allow/ban, launch/cancel, confirm/deny, support/oppose and equivalent polarity. Preserve numbers, dates, entities, causal relationships and attribution. Return ONLY the requested JSON object.';
const isHardQuota=(status,message='')=>status===429&&/free-models-per-day|daily quota|daily limit|quota_exceeded|exceeded your current quota|monthly quota|credit_balance_exhausted|insufficient_quota|no credits remaining|payment_required/i.test(String(message));
const request=async(provider,prompt,system=commonSystem,formats=articleFormats,options={})=>{const maxCompletionTokens=Number(options.maxCompletionTokens)||2800;for(let attempt=0;attempt<=MAX_TRANSIENT_RETRIES;attempt++){const signal=AbortSignal.timeout(providerTimeout(provider));let r;if(provider==='Groq')r=await fetch('https://api.groq.com/openai/v1/chat/completions',{method:'POST',signal,headers:{Authorization:`Bearer ${process.env.GROQ_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:providerModel(provider),max_completion_tokens:maxCompletionTokens,reasoning_effort:'low',response_format:formats.Groq,messages:[{role:'system',content:system},{role:'user',content:prompt}]})});else if(provider==='Gemini')r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${providerModel(provider)}:generateContent`,{method:'POST',signal,headers:{'x-goog-api-key':process.env.GEMINI_API_KEY,'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:`${system}\n\n${prompt}`}]}],generationConfig:{responseMimeType:'application/json',responseSchema:{type:'OBJECT',properties:{title:{type:'STRING'},description:{type:'STRING'},content:{type:'STRING'}},required:['title','description','content'],propertyOrdering:['title','description','content']},maxOutputTokens:maxCompletionTokens}})});else if(provider==='OpenRouter')r=await fetch('https://openrouter.ai/api/v1/chat/completions',{method:'POST',signal,headers:{Authorization:`Bearer ${process.env.OPENROUTER_API_KEY}`,'Content-Type':'application/json','HTTP-Referer':'https://github.com/WebTooler/Trendforge','X-Title':'TrendForge'},body:JSON.stringify({model:providerModel(provider),max_tokens:maxCompletionTokens,response_format:formats.OpenRouter,plugins:[{id:'response-healing'}],provider:{allow_fallbacks:true,sort:'latency'},messages:[{role:'system',content:system},{role:'user',content:prompt}]})});else if(provider==='Cohere')r=await fetch('https://api.cohere.com/v2/chat',{method:'POST',signal,headers:{Authorization:`Bearer ${process.env.COHERE_API_KEY}`,'Content-Type':'application/json','X-Client-Name':'TrendForge'},body:JSON.stringify({model:providerModel(provider),max_tokens:maxCompletionTokens,temperature:0.1,seed:42,thinking:{type:'disabled'},response_format:formats.Cohere,messages:[{role:'system',content:system},{role:'user',content:`Generate a JSON object with exactly the requested fields. ${prompt}`}]})});else throw new Error(`Unsupported provider: ${provider}`);if(r.ok){const j=await r.json();if(provider==='Gemini')return {text:j.candidates?.[0]?.content?.parts?.map(p=>p.text||'').join('')||'',usage:j.usageMetadata||null};if(provider==='Cohere'){const content=j.message?.content;const textBlocks=Array.isArray(content)?content.filter(x=>x?.type==='text').map(x=>x.text||'').join(''):(typeof content==='string'?content:'');const thinkingBlocks=Array.isArray(content)?content.filter(x=>x?.type==='thinking').length:0;return {text:textBlocks,usage:j.usage||null,diagnostics:{contentTypes:Array.isArray(content)?content.map(x=>x?.type||'unknown'):[],thinkingBlocks}};}return {text:j.choices?.[0]?.message?.content||'',usage:j.usage||null};}const body=(await r.text()).slice(0,1200);const hardQuota=isHardQuota(r.status,body);if(!hardQuota&&r.status>=500&&attempt<MAX_TRANSIENT_RETRIES){await sleep(retryDelay(r,attempt));continue;}throw new Error(`${r.status}: ${body}`);}throw new Error('Provider request exhausted');};
const parseWriterJson=raw=>{const cleaned=String(raw).trim().replace(/^```(?:json)?\s*/i,'').replace(/\s*```$/i,'');const candidates=[cleaned];const start=cleaned.indexOf('{'),end=cleaned.lastIndexOf('}');if(start>=0&&end>start)candidates.push(cleaned.slice(start,end+1));for(const candidate of candidates){if(!candidate)continue;try{const parsed=JSON.parse(candidate);if(parsed&&typeof parsed==='object')return parsed;}catch{}}return null;};
const normalize=word=>{let w=word.toLowerCase();if(w.length>5&&w.endsWith('ies'))w=w.slice(0,-3)+'y';else if(w.length>5&&w.endsWith('ing'))w=w.slice(0,-3);else if(w.length>5&&w.endsWith('ed'))w=w.slice(0,-2);else if(w.length>4&&w.endsWith('s'))w=w.slice(0,-1);return w;};
const topicTokens=text=>{const stop=new Set(['about','after','again','also','been','being','could','from','have','into','more','most','over','said','some','than','that','their','there','these','they','this','what','when','which','with','will','would','your','technology','tech','digital','latest','news','update','today','company','industry','development','developments','article','story']);return new Set(String(text).toLowerCase().replace(/[^a-z0-9]+/g,' ').split(/\s+/).filter(w=>w.length>=4&&!stop.has(w)).map(normalize));};
const entityTokens=text=>{const known=['openai','anthropic','google','microsoft','meta','amazon','apple','nvidia','tesla','xai','mistral','deepmind','bitcoin','ethereum','xrp','solana','clarity act','fed','senate','india','indian','consumer regulator','general motors','gm','carplay','flock','chargepoint','ev charger'];const lower=String(text).toLowerCase();return new Set(known.filter(x=>lower.includes(x)));};
const requestedTitle=prompt=>{const m=String(prompt).match(/^Title:\s*(.+)$/im);return(m?.[1]||'').replace(/\s+-\s+(Reuters|Yahoo Finance|finance\.yahoo\.com|CNBC|The Guardian|TechCrunch|GeekWire|Mistral|Microsoft|Apple)$/i,'').trim();};
const loadEvidenceBlueprint=expectedTitle=>{
  try{
    const raw=JSON.parse(fs.readFileSync('data/pre-writer-pipeline.json','utf8'));
    const row=(raw.candidates||[]).find(x=>String(x.title||'').trim()===String(expectedTitle||'').trim());
    return row?.readyForWriter ? {blueprint:row.evidence?.blueprint||null,factMap:row.evidence?.storyFactMap||row.evidence?.editorialEvidenceBrief?.storyFactMap||null} : null;
  }catch{return null;}
};
const normalizeGeneratedDraft=draft=>{
  const out={...draft};
  const text=String(out.content||'');
  const parts=text.match(/[^.!?]+[.!?](?:\\s|$)/g)||[];
  const cleaned=[]; let removed=0;
  for(const sentence of parts){
    const norm=sentence.toLowerCase().replace(/[^a-z0-9\\s]/g,'').replace(/\\s+/g,' ').trim();
    const prev=cleaned.length?cleaned[cleaned.length-1].toLowerCase().replace(/[^a-z0-9\\s]/g,'').replace(/\\s+/g,' ').trim():'';
    if(norm&&norm===prev){removed++;continue;}
    cleaned.push(sentence);
  }
  if(removed) out.content=cleaned.join('').trim();
  const desc=String(out.description||'').trim();
  if(desc.length>320){
    const limit=desc.slice(0,320).trim();
    const sentenceCut=Math.max(limit.lastIndexOf('.'),limit.lastIndexOf('!'),limit.lastIndexOf('?'),limit.lastIndexOf('…'));
    if(sentenceCut>=100) out.description=limit.slice(0,sentenceCut+1).trim();
    else { const wordCut=limit.replace(/\\s+\\S*$/,'').trim(); if(wordCut.length>=100) out.description=`${wordCut}…`; }
  }
  return {draft:out,removedDuplicateSentences:removed};
};
const topicAlignment=(requested,draft)=>{const source=String(requested||'');const generated=`${draft.title} ${draft.description} ${draft.content.slice(0,7000)}`;const a=topicTokens(source),b=topicTokens(generated),titleB=topicTokens(draft.title);const shared=[...a].filter(x=>b.has(x)),titleShared=[...a].filter(x=>titleB.has(x));const entities=[...entityTokens(source)].filter(x=>entityTokens(generated).has(x));const titleLower=draft.title.toLowerCase(),requestedLower=source.toLowerCase();const titleExactConcept=!!requestedLower&&(titleLower.includes(requestedLower.slice(0,Math.min(32,requestedLower.length)))||requestedLower.includes(titleLower.slice(0,Math.min(32,titleLower.length))));return{score:shared.length,shared:shared.slice(0,12),titleShared:titleShared.slice(0,12),entities:entities.slice(0,8),passed:titleShared.length>=1&&shared.length>=2||titleShared.length>=2||shared.length>=4||entities.length>=1||titleExactConcept};};

export async function generateWithTrendForgeWriter({prompt,category='Technology',expectedTitle=''}){
  const inferred=category&&category!=='Technology'?category:prompt.match(/(?:category|section)\s*[:=]\s*(AI|Technology|How-To|Innovation|Product Launches|Digital Life|Crypto)/i)?.[1]||category;
  const contract=buildWriterContract(inferred);
  const evidenceModel=loadEvidenceBlueprint(expectedTitle);
  const blueprint=evidenceModel?.blueprint||null;
  const factMap=evidenceModel?.factMap||null;
  if(!factMap||factMap.version!==1||!factMap.capacity||!Array.isArray(factMap.coreFacts)) throw new Error('Canonical Story Fact Map missing or invalid; writer generation blocked.');
  const wordGuide=blueprint?.targetWords||{min:WRITER_TARGET_MIN_WORDS,max:WRITER_TARGET_MAX_WORDS,soft:600};
  const h2Guide=blueprint?.h2Guidance||{preferredMin:2,preferredMax:3};
  const hardWordMin=Math.max(MIN_WRITER_WORDS,Number(wordGuide.min||WRITER_TARGET_MIN_WORDS));
  const hardWordMax=Math.min(1100,Math.max(hardWordMin,Number(wordGuide.max||WRITER_TARGET_MAX_WORDS)));
  const claimBudget=Number(factMap?.capacity?.maxFactualClaims||blueprint?.evidenceCapacity?.maxFactualClaims||0);
  const maxFactualClaims=claimBudget>0?claimBudget:0;
  const factGuide=Array.isArray(factMap?.coreFacts)?factMap.coreFacts.slice(0,14).map(f=>`[${f.factId}] ${f.text} (source=${f.sourceId}, role=${f.sourceRole})`).join('\\n'):'';
  const claimBudgetGuide=blueprint&&maxFactualClaims?[
    'EVIDENCE FACT MAP — HARD BOUNDARY:',
    '- Core story facts are the only allowed factual building blocks for the main narrative.',
    '- Context facts may be used only when explicitly framed as context and must never substitute for missing core facts.',
    '- Every material factual sentence must map to one or more Fact IDs from the fact map.',
    '- Do not create a new factual proposition by combining unrelated facts.',
    `- Evidence claim capacity is a planning boundary, not a sentence-count quota: ${maxFactualClaims} supported claim slots are available for planning; do not treat this as a sentence limit.`,
    '- Group related facts into coherent paragraphs and synthesize supported details; do not mechanically enumerate Fact IDs or force one sentence per fact.',
    '- If the fact map cannot support the requested depth, write the shortest complete article allowed by the evidence capacity; never add facts from memory.',
    '',
    'CORE FACTS:',
    factGuide
  ].join('\\n') : '';
  const architectureGuide=blueprint ? [
    'ARTICLE ARCHITECTURE — derive the structure from evidence before drafting:',
    `- Evidence mode: ${blueprint?.mode||'standard'}.`,
    `- Evidence-supported word range: ${wordGuide.min}-${wordGuide.max} (soft ${wordGuide.soft}).`,
    `- H2 STRUCTURE: recommended range is ${Number(h2Guide.preferredMin||2)}-${Number(h2Guide.preferredMax||blueprint?.maxH2||3)} H2 headings. This is advisory, not a hard quota; a shorter complete section is valid when it has a distinct substantive job. Never create a heading or pad a section merely to hit a word target.`,
    '- Give each H2 one distinct job: development, evidence/details, implications/context, limitations/uncertainty, or supported next steps.',
    '- Do not repeat the same fact in multiple sections merely to increase length.',
    '- Do not create an H2 for a single trivial sentence or unsupported context.',
    '- Opening paragraph must establish the concrete development without repeating the headline.',
    '- Final section must add a distinct takeaway, limitation, uncertainty, or supported next step; never use a generic conclusion.',
    ...(blueprint?.synthesis?.allowed?[`SYNTHESIS ENDING CONTRACT:`, `- End with ${blueprint.synthesis.heading||'What the Evidence Shows'} when useful.`, `- Maximum synthesis words: ${Number(blueprint.synthesis.maxWords||0)}; maximum synthesis statements: ${Number(blueprint.synthesis.maxStatements||0)}.`, `- Synthesis may reuse ONLY Fact IDs: ${(blueprint.synthesis.allowedFactIds||[]).join(', ')}.`, `- Every synthesis statement must be a paraphrase or combination of already established facts.`, `- Never introduce a new fact, number, date, entity, cause, prediction, stronger certainty, or attribution in the synthesis.`]:[]),
    '- Prefer fewer strong sections over many thin sections. Aim for roughly 60+ words when the evidence supports it, but treat that as a writing target, not a validation quota.'
  ].join('\\n') : '';
  const enginePrompt=`${contract}\n\n${architectureGuide}\n\n${claimBudgetGuide}\n\nRESEARCH / ARTICLE BRIEF:\n${prompt}\n\nSTRICT EVIDENCE WRITING CONTRACT:\n1. The retrieved evidence pack is the ONLY factual knowledge you may use. Generic background knowledge is also forbidden, even when it is commonly true.\n2. Before writing each factual sentence, silently map it to one or more exact evidence passages. If no passage supports the full sentence, DELETE the sentence. Every checkable fact needs direct passage support.\n2a. Do not state what a product, company, technology, regulation, market, or person normally does unless that fact is explicitly in the evidence. Do not turn an implication, context clue, headline, URL, source metadata, or your own reasoning into a factual statement.\n2b. Cross-source synthesis may combine supported facts, but it must not create a new causal relationship, comparison, motive, outcome, capability, chronology, or generalization.\n2c. Treat syndicated or republished sources as one underlying story for corroboration; repeated wording across publishers is not independent confirmation.\n3. Never infer a product specification, comparison, motive, effect, user reaction, future outcome, price, date, rating, compatibility detail, competitor comparison, or market implication that is not explicit in the evidence.\n4. Do not combine passages into a stronger claim than either passage supports. Preserve attribution.\n5. POLARITY LOCK: preserve the exact direction of every factual relationship. Never turn increased into decreased, reduced into increased, rose into fell, gain into loss, approved into rejected, allowed into banned, launched into cancelled, confirmed into denied, or supports into opposes. Do not strengthen or weaken a factual relationship while paraphrasing.\n6. Preserve every material number, date, named entity, causal relationship and attribution. If the evidence does not support an exact value or relationship, omit the claim rather than guess.\n7. Avoid speculative future language unless the evidence explicitly states that possibility.\n8. Recommendations may be opinion only and must add no new factual premise.\n9. Evidence capacity mode: ${blueprint?.mode||'standard'}. HARD WORD RANGE: ${hardWordMin}-${hardWordMax} words. Aim for ${Math.min(hardWordMax,Math.max(hardWordMin,Number(wordGuide.soft||600)))} words. The evidence-relative minimum is a capacity target, not a license to invent facts; if a complete article must be shorter, prefer a truthful shorter article and let downstream depth rules decide. Never pad with unsupported facts.\n10. H2 count follows the evidence-derived recommendation in the blueprint. The H2 count is advisory, not a hard ceiling. Add an extra H2 when it has a distinct substantive job; never add H2 headings just to increase length. Fewer strong sections are preferred.\n11. The requested story title is the hard topic boundary. Do not switch stories.\n12. Return only JSON with exactly title, description and content.\nFINAL SELF-CHECK: For every factual sentence, verify source support, polarity/direction, numbers, dates, entities, causal relationship and attribution. Delete any sentence that fails any check.`;
  const requested=expectedTitle||requestedTitle(prompt);let candidateAttempts=0;let lastFailure='';const candidateIndex=Math.max(1,Number(process.env.TRENDFORGE_WRITER_CANDIDATE_INDEX||1));let writerRotationCursor=(candidateIndex-1)%Math.max(1,available.length);
  if(loadBudget('writer').attempts>=MAX_PROVIDER_ATTEMPTS_PER_RUN)throw new Error('TrendForge Writer Engine: run-level AI provider budget exhausted.');
  const providerOrder=available.length?available.map((_,i)=>available[(writerRotationCursor+i)%available.length]):[];for(const provider of providerOrder){
    if(!process.env[keyFor(provider)])continue;
    if(candidateAttempts>=Math.min(MAX_PROVIDER_ATTEMPTS_PER_CANDIDATE,candidateAttemptCap()))break;
    const budget=loadBudget('writer');if(budget.attempts>=MAX_PROVIDER_ATTEMPTS_PER_RUN)break;budget.attempts+=1;candidateAttempts+=1;saveBudget(budget,'writer');console.log(`TrendForge Writer Engine: provider attempt ${budget.attempts}/${MAX_PROVIDER_ATTEMPTS_PER_RUN} (candidate ${candidateAttempts}/${MAX_PROVIDER_ATTEMPTS_PER_CANDIDATE}) — ${provider}.`);const started=Date.now();
    try{const recoveryPrompt=lastFailure?`\n\nQUALITY RECOVERY — A previous provider failed these deterministic checks: ${lastFailure}. Correct every listed issue in your output. Do not repeat the failed structure. Ensure the final article is within the HARD WORD RANGE and satisfies the evidence-mode paragraph depth. Do not pad. H2 count follows the evidence-derived recommendation; fewer strong sections are preferred.\n`:'';
      const completionBudget=Math.max(650,Math.min(1600,Math.ceil((hardWordMax+Number(blueprint?.synthesis?.maxWords||0))*1.45)+220));
      const response=await request(provider,enginePrompt+recoveryPrompt,commonSystem,articleFormats,{maxCompletionTokens:completionBudget});const text=typeof response==='string'?response:response.text;const usage=typeof response==='object'?response.usage:null;if(usage)console.log(`TrendForge Writer Engine: ${provider} usage ${JSON.stringify(usage)}.`);if(response?.diagnostics)console.log(`TrendForge Writer Engine: ${provider} response diagnostics ${JSON.stringify(response.diagnostics)}.`);if(!text.trim()){mark(provider,200,'Empty model response');console.log(`TrendForge Writer Engine: ${provider} returned an empty writer response after ${Date.now()-started}ms; no downstream validation was attempted.`);continue;}let draft=parseWriterJson(text);if(!draft||typeof draft.title!=='string'||typeof draft.description!=='string'||typeof draft.content!=='string'){mark(provider,200,'Invalid structured output');console.log(`TrendForge Writer Engine: ${provider} returned non-empty but invalid structured output after ${Date.now()-started}ms.`);continue;}const normalized=normalizeGeneratedDraft(draft);draft=normalized.draft;if(normalized.removedDuplicateSentences)console.log(`TrendForge Writer Engine: ${provider} removed ${normalized.removedDuplicateSentences} exact consecutive duplicate sentence(s) deterministically before validation.`);const alignment=topicAlignment(requested,draft);if(requested&&!alignment.passed){console.log(`TrendForge Writer Engine: ${provider} output rejected for topic drift after ${Date.now()-started}ms.`);continue;}const validation=validateDraft({title:draft.title,description:draft.description,content:draft.content,category:inferred,blueprint,maxFactualClaims,evidenceFactMap:factMap});const neutrality=validatePoliticalNeutrality({title:draft.title,description:draft.description,content:draft.content});if(!neutrality.passed){lastFailure=`political neutrality: ${neutrality.reasons.join('; ')}`;console.log(`TrendForge Writer Engine: ${provider} output rejected before publication — political neutrality: ${neutrality.reasons.join('; ')}.`);continue;}if(!validation.passed){lastFailure=validation.errors.join('; ');console.log(`TrendForge Writer Engine: ${provider} output rejected before publication — ${validation.errors.join('; ')}.`);continue;}if(validation.metrics.words<hardWordMin){console.log(`TrendForge Writer Engine: ${provider} output rejected before publication — word count ${validation.metrics.words}; mode minimum ${hardWordMin}.`);continue;}console.log(`TrendForge Writer Engine: ${provider} produced ${validation.metrics.words} words.`);markSuccess(provider,{latencyMs:Date.now()-started,usage});return{text:JSON.stringify(draft),provider,policyVersion:'2.7',topicAlignment:alignment};}catch(e){const message=e instanceof Error?e.message:String(e);const status=Number(message.match(/^(\d+)/)?.[1]||0);mark(provider,status,message);console.log(`TrendForge Writer Engine: ${provider} failed [${status||'network'}] — ${message.slice(0,260)}.`);if(isHardQuota(status,message))break;}}
  throw new Error('TrendForge Writer Engine: no provider produced a policy-valid article.');
}

export async function generateWithTrendForgeRepair({prompt}){
  const repairSystem='You are the TrendForge Atomic Grounding Repair Engine. The supplied publisher evidence is the sole factual source. Return ONLY JSON with a repairs array. Never rewrite an article and never invent facts. Preserve factual polarity exactly: never reverse increased/decreased, rise/fall, gain/loss, approve/reject, allow/ban, launch/cancel, confirm/deny, support/oppose, numbers, dates, entities, causal relationships or attribution.';
  let attempts=0;
  if(loadBudget('repair').attempts>=MAX_REPAIR_PROVIDER_ATTEMPTS)throw new Error('TrendForge repair: run-level repair provider budget exhausted.');
  for(const provider of available){
    if(!process.env[keyFor(provider)])continue;
    if(attempts>=MAX_REPAIR_PROVIDER_ATTEMPTS)break;
    const budget=loadBudget('repair');if(budget.attempts>=MAX_REPAIR_PROVIDER_ATTEMPTS)break;budget.attempts+=1;attempts+=1;saveBudget(budget,'repair');console.log(`TrendForge Atomic Repair: provider attempt ${budget.attempts}/${MAX_REPAIR_PROVIDER_ATTEMPTS} (${attempts}/${MAX_REPAIR_PROVIDER_ATTEMPTS}) — ${provider}.`);const started=Date.now();
    try{const response=await request(provider,prompt,repairSystem,repairFormats,{maxCompletionTokens:1600});const text=typeof response==='string'?response:response.text;const usage=typeof response==='object'?response.usage:null;if(usage)console.log(`TrendForge Atomic Repair: ${provider} usage ${JSON.stringify(usage)}.`);if(!text.trim()){mark(provider,200,'Empty atomic repair response');continue;}const parsed=parseWriterJson(text);if(!parsed||!Array.isArray(parsed.repairs)){mark(provider,200,'Invalid repair structured output');continue;}const repairs=parsed.repairs.filter(x=>x&&typeof x.original==='string'&&typeof x.replacement==='string');if(!repairs.length){mark(provider,200,'No repair objects');continue;}markSuccess(provider,{latencyMs:Date.now()-started,usage});console.log(`TrendForge Atomic Repair: ${provider} returned ${repairs.length} atomic sentence repair(s) after ${Date.now()-started}ms.`);return{text:JSON.stringify({repairs}),provider,policyVersion:'1.1'};}catch(e){const message=e instanceof Error?e.message:String(e);const status=Number(message.match(/^(\d+)/)?.[1]||0);mark(provider,status,message);console.log(`TrendForge Atomic Repair: ${provider} failed [${status||'network'}] — ${message.slice(0,260)}.`);if(isHardQuota(status,message))break;}}
  throw new Error('TrendForge Atomic Repair: no provider produced valid sentence repairs.');
}
