# Image V3 Visual QA

A candidate is production-eligible only after technical QA and vision/editorial QA are both PASS. Technical checks cover file integrity, dimensions, size and OCR-detected text artifacts. Vision/editorial checks cover story relevance, subject clarity, composition, factual visual grounding, and brand/logo artifacts.

`UNASSESSED` is not a pass and must never be promoted to production.
